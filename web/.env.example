# Cloudflare Worker base URL used by the "Cloudflare Edge" backend toggle.
# Copy this file to `.env.local` and set your deployed Worker URL.
# If unset, the app falls back to a placeholder and surfaces a clear error when
# the Edge backend is selected.
VITE_WORKER_URL=https://edge-sentiment.<your-subdomain>.workers.dev
