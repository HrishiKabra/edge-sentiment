{
  "name": "edge-sentiment-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker edge gateway for EdgeSentiment — CORS + proxy to the off-loaded ONNX inference service.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241127.0",
    "typescript": "5.6.3",
    "wrangler": "3.91.0"
  }
}
