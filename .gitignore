# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# --- Models / large binaries (trained + exported artifacts) ---
training/models/
training/**/checkpoints/
*.onnx
*.zip
*.onnx.data
# ONNX/quantization temp external-data files (UUID-named, written to cwd)
training/????????-????-????-????-????????????

# --- Web ---
web/node_modules/
web/dist/
web/public/model/*.onnx
# ...except the shipped INT8 model, committed so the Pages CI build can serve it
# (64 MB, under GitHub's 100 MB/file limit).
!web/public/model/distilbert-sst2-int8.onnx
*.tsbuildinfo

# --- Tooling scratch ---
.playwright-mcp/

# --- Worker ---
worker/node_modules/
worker/.wrangler/
worker/.dev.vars

# --- Inference service ---
inference-service/node_modules/

# --- OS / editor ---
.DS_Store
.idea/
.vscode/

# local web env overrides
web/.env.local

# Claude Code local tooling state
.claude/
